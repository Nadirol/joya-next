export interface tour {
    destinations: string | null,
    image: string,
    title: string,
    duration: number | null,
    price: number | null,
    id: string,
    tourType: string,
    description: string | null
}